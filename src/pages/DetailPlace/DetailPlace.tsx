import React from 'react';
import './DetailPlace.scss'
import { Typography, Row, Col, Card, Space, Button } from "antd";


const DetailPlace:React.FC = () => {
    return (
        <div className='container-detail-place'>
            detail place
        </div>
    );
};

export default DetailPlace;